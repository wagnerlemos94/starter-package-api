spring:
  datasource:
    url: jdbc:h2:mem:starter-package;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false

security:
  jwt:
    secret: chave-de-teste-com-pelo-menos-trinta-e-dois-bytes
    expiration: 3600000
